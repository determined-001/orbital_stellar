{
  "name": "@orbital-stellar/worker-core",
  "version": "0.1.0",
  "description": "Worker idempotency primitives for Orbital",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "migrations",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:integration": "INTEGRATION_TESTS=true vitest run test/integration",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "pg": "^8.22.0",
    "vitest": "^4.1.11"
  }
}
